spring.application.name=backend
server.port=8080

# Database
spring.datasource.url=jdbc:mysql://localhost:3306/vibha?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=Mk2442002#ssg
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.show-sql=false
logging.level.org.hibernate.SQL=INFO

# CORS
app.cors.allowed-origins=http://localhost:5173,http://localhost:5174,http://localhost:5175
app.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
app.cors.allowed-headers=*

# Security/JWT
app.security.jwt.secret=mySuperSecretKeyForJwtToken123456789
app.security.jwt.expiration-ms=86400000

# Logging
logging.level.org.springframework.security=INFO

# Uploads
app.upload.dir=uploads